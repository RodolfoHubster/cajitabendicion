# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
.env.*
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Plantilla de variables de entorno (la regla .env.* de arriba la excluiria)
!.env.example

# Claude Code: el andamiaje del proyecto SI se versiona (instrucciones,
# reglas por ruta, comandos, subagentes y permisos), para que valga igual
# en cualquier maquina y quede como parte del entregable. Lo que se queda
# fuera es lo personal de cada quien y lo que puede traer secretos.
.claude/*
!.claude/rules/
!.claude/commands/
!.claude/agents/
!.claude/settings.json
.claude/settings.local.json
.claude/launch.json
.sesion-prueba.json
